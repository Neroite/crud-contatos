@model RedefinirSenhaModel
@{
    Layout = "_LayoutDeslogado";
    ViewData["Title"] = "Redefinir a senha";
}

@if (TempData["MensagemErro"] != null)
{
    <div class="alert alert-danger" role="alert" style="text-align:center">
        <button type="button" class="btn btn-danger btn-sm close-alert" arial-label="Close">X</button>
        @TempData["MensagemErro"]
    </div>

}

<form asp-controller="Login" asp-action="EnviarLinkPararRedefinirSenha" method="post">
    <br>
    <div class="container">
        <div class="row d-flex justify-content-center align-items-center h-100">
            <div class="col-12 col-md-8 col-lg-6 col-xl-5">
                <div class="card" style="border-radius: 1rem;">
                    <div class="card-body p-3 text-center">
                        <div class="mb-md-5 mt-md-4 pb-5">
                            <h2 class="fw-bold mb-2 text-uppercase">Login</h2>

                            <div class="form-outline form-white mb-4">
                                <input type="text" asp-for="Login" class="form-control form-control-lg" id="login" />
                                @Html.ValidationMessageFor(x => x.Login)
                            </div>
                            <br>
                            <div class="form-outline form-white mb-4">
                                <h2 for="email" class="fw-bold mb-2 text-uppercase">Email</h2>
                                <input type="text" asp-for="Email" type="email" id="email" class="form-control form-control-lg" />
                                @Html.ValidationMessageFor(x => x.Email)
                            </div>
                            <br>
                            <br>
                            <button class="btn btn-outline-light btn-lg px-4" type="submit">Redefinir a senha</button>
                            <br>
                        </div>
                        <a class="btn btn-secondary w-100" asp-controller="Contato" asp-action="Index" role="button">Voltar</a>

                    </div>
                </div>
            </div>
        </div>
    </div>
</form>